package com.laszlobogacsi.albimester.users;

import org.junit.jupiter.api.Test;

import static org.mockito.Mockito.*;

public class UserServiceTest {
//    @Test
//    void canCreateAnOwnerUser() {
//        UserRepository userRepository = mock(UserRepository.class);
//        UserService userService = new UserService();
//        UserDto ownerUser = new UserDto("FirstName", "LastName", "email@example.com", "owner");
//        userService.create(ownerUser);
//        verify(userRepository, times(1)).save(ownerUser);
//    }
}
